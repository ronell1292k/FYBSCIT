#include<stdio.h>
#include<conio.h>

void main()
  {
  int a,b,add,sub,mul,div,mod;
  printf("Enter the values of a and b");
  scanf("%d%d",&a,&b);
  add=a+b;
  sub=a-b;
  mul=a*b;
  div=a/b;
  mod=a%b;
  printf("Add=%d\n",add);
  printf("Sub=%d\n",sub);
  printf("Mul=%d\n",mul);
  printf("Div=%d\n",div);
  printd("Mod=%d\n",mod);
  getch()
  }
